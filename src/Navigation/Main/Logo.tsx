import styled from "@emotion/styled";

export const Logo = () => {
  return (
    <HeaderContainer>
      <div>LOGO</div>
    </HeaderContainer>
  );
};

const HeaderContainer = styled.div({
  backgroundColor: "#FAF9F6",
  margin: "10px 30px",
});
